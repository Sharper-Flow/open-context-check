---
description: Analyze context window token usage and overhead
---

Analyze the current OpenCode session's context window overhead by running:

!`node "${OPEN_CONTEXT_CHECK:-$HOME/dev/oc-plugins/open-context-check/dist/context-check.js}" $ARGUMENTS`

Show the output to the user. If there are recommendations, briefly explain each one and suggest concrete next steps.
